#include <iostream>

using namespace std;

int main() {

    double *sales = nullptr,
    total = 0.0,
    avrage;
    int numDays, count;

    cout << "How many days of sales fugures do you wish ";
    cout << "to process? ";
    cin >> numDays;

    sales = new double[numDays];
    

    return 0;
}